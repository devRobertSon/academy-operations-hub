{
  "name": "academy-operations-hub",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "node --test",
    "check": "node scripts/qa.mjs --static-only",
    "qa": "node --test && node scripts/qa.mjs"
  }
}
